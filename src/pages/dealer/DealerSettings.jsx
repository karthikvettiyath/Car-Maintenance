import { DealerLayout } from '../../components/layout/DealerLayout';

const DealerSettings = () => {
    return (
        <DealerLayout>
            <h1 className="text-3xl font-bold text-white mb-6">Dealer Settings</h1>
            <p className="text-slate-400">Manage your dealership profile and preferences.</p>
        </DealerLayout>
    );
};

export default DealerSettings;
